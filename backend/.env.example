# Local development with SQLite:
DJANGO_DEBUG=1
# USE_SQLITE=1

# Local development against the Railway Postgres database:
# DJANGO_DEBUG=1
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME

# Production should set DATABASE_URL in Railway variables.
# Do not commit real secrets in this file.
